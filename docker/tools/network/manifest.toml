[tool]
name = "network"
description = "Network diagnostics and TCP/UDP helpers"
version = "1"
tier = "standard"

[size]
installed = "15MB"
download = "5MB"

[dependencies]
requires = []
conflicts = []

[install]
method = "apt"
packages = ["netcat-openbsd", "socat", "dnsutils", "traceroute"]

[capabilities]
provides = ["nc", "socat", "dig", "traceroute"]
