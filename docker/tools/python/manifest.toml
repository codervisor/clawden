[tool]
name = "python"
description = "Python 3 runtime with pip and venv"
version = "3.11"
tier = "standard"

[size]
installed = "120MB"
download = "40MB"

[dependencies]
requires = []
conflicts = []

[install]
method = "apt"
packages = ["python3", "python3-pip", "python3-venv"]

[capabilities]
provides = ["python3", "pip3", "venv"]
