[tool]
name = "sandbox"
description = "Bubblewrap-based execution sandbox"
version = "0.9"
tier = "standard"

[size]
installed = "5MB"
download = "2MB"

[dependencies]
requires = []
conflicts = []

[install]
method = "apt"
packages = ["bubblewrap"]

[capabilities]
provides = ["bwrap", "clawden-sandbox"]
