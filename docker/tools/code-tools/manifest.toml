[tool]
name = "code-tools"
description = "Fast code search and file inspection tools"
version = "1"
tier = "standard"

[size]
installed = "25MB"
download = "8MB"

[dependencies]
requires = []
conflicts = []

[install]
method = "apt"
packages = ["ripgrep", "fd-find", "bat"]

[capabilities]
provides = ["rg", "fdfind", "bat"]
