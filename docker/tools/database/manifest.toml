[tool]
name = "database"
description = "SQLite CLI and shared libraries"
version = "3"
tier = "standard"

[size]
installed = "5MB"
download = "2MB"

[dependencies]
requires = []
conflicts = []

[install]
method = "apt"
packages = ["sqlite3"]

[capabilities]
provides = ["sqlite3"]
