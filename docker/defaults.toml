# ClawDen Docker Defaults
# Override via environment variables or mounted config files.

[general]
log_level = "info"
workspace = "/home/clawden/workspace"

[openclaw]
port = 18789
health_endpoint = "/health"

[zeroclaw]
port = 42617
health_endpoint = "/health"
